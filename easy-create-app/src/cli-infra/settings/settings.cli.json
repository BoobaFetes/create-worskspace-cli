{
  "name": "easy-create-app",
  "description": "'easy-create-app' cli helps to create a react app in a monorepo. Project use clean architecture and are ready to debug and publish.",
  "version": "1.0.0",
  "commands": {
    "schema": {
      "description": "get or validate template of schema",
      "commands": {
        "get": {
          "description": "get template of schema",
          "argument": {
            "name": "<string>",
            "description": "the folder where template will be copied"
          }
        },
        "validate": {
          "description": "validate template of schema",
          "argument": {
            "name": "<string>",
            "description": "the name of the schema to use"
          },
          "options": [
            {
              "flags": "-t <schemaFile>, --target <schemaFile>",
              "description": "repository of your schema to load"
            }
          ]
        }
      }
    },
    "create": {
      "argument": {
        "name": "<string>",
        "description": "the name of the schema to use"
      },
      "options": []
    },
    "update": {
      "argument": {
        "name": "<string>",
        "description": "the name of the schema to use"
      },
      "options": []
    }
  }
}
